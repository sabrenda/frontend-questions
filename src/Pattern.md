<div align="center"><h1>Pattern</h1></div>

[⚙️ Back to menu](../README.md)

 <div id="menu"></div>

| No.|             Вопрос                   |
|:---|:-------------------------------------|
|1| [`MVP`](#a1)|MVC
|2| [`MVC`](#a2)|
|3| [Паттерны проектирования](#a3)|
|4| [DRY (Don't Repeat Yourself):  Не повторяться](#a4)|
|5| [KISS (Keep It Simple, Stupid): Быть проще](#a5)|
|6| [YAGNI (You Aren’t Gonna Need It) / Вам это не понадобится](#a6)|
|7| [Big Design Up Front (BDUF) / Глобальное проектирование прежде всего](#a7)|
|8| [Avoid Premature Optimization / Избегайте преждевременной оптимизации](#a8)|
|9| [Бритва Оккама / Не создавайте ненужных сущностей без необходимости](#a9)|
|10| [GoF (Gang of Four) (паттерны проектирования)](#a10)|
|11| [GRASP (General Responsibility Assignment Software Patterns)](#a11)|
|12| [](#a12)|
|13| [](#a13)|




<div id="a1"></div>

## 1. MVP

**Model View Presenter** - MVP позволяет ускорить разработку и разделить ответственность разных специалистов; приложение удобнее тестировать и поддерживать. Model (Модель) работает с данными, проводит вычисления и руководит всеми бизнес-процессами. View (Вид или представление) показывает пользователю интерфейс и данные из модели. Presenter (Представитель) служит прослойкой между моделью и видом 

[Оглавление - Задачи 🔼](#menu)

<div id="a2"></div>

## 2. MVC

**MVC** – model view controller - это шаблон, который разделяет логику приложения на три части: Model (модель). Получает данные от контроллера, выполняет необходимые операции и передаёт их в вид. View (представление). Получает данные от модели и выводит их для пользователя. Интерфейс. Controller (контроллер). Обрабатывает действия пользователя, проверяет полученные данные и передаёт их модели. Изменить данные, сделать запрос. 

[Оглавление - Задачи 🔼](#menu)

<div id="a3"></div>

## 3. Паттерны проектирования

**`Singleton`** - одиночка, порождающий шаблон проектирования, гарантирующий, что в приложении будет единственный экземпляр некоторого класса, и предоставляющий глобальную точку доступа к этому экземпляру. Данный шаблон необходимо применять, когда необходим лишь один экземпляр класса, например для методов, которые описывают конфигурацию. 

**`Factory Method`** - порождающий шаблон проектирования, предоставляющий подклассам (дочерним классам) интерфейс для создания экземпляров некоторого класса. В момент создания наследники могут определить, какой класс создавать. Иными словами, данный шаблон делегирует создание объектов наследникам родительского класса. Это позволяет использовать в коде программы не специфические классы, а манипулировать абстрактными объектами на более высоком уровне. 

**`Abstract Factory`** - Шаблон реализуется созданием абстрактного класса Factory, который представляет собой интерфейс для создания компонентов системы. Затем пишутся классы, реализующие этот интерфейс. 

**`Prototype`** - прототип, порождающий шаблон проектирования. Задаёт виды создаваемых объектов с помощью экземпляра-прототипа и создаёт новые объекты путём копирования этого прототипа.

**`Builder`** - строитель, порождающий шаблон проектирования предоставляет способ создания составного объекта. Отделяет конструирование сложного объекта от его представления так, что в результате одного и того же процесса конструирования могут получаться разные представления. 

В "**`Observer`**" существует две основные роли: Субъект/Subject и Наблюдатель/Observer Паттерн "Наблюдатель" определяет отношение один ко многим (one-to-many), позволяя нескольким объектам-наблюдателям одновременно следить за наблюдаемым субъектом. При изменении состояния наблюдаемого субъекта об этом уведомляются все объекты-наблюдатели, чтобы они, в свою очередь, могли обновить собственное состояние. 

**`Реактивность`** - это способность фрагмента кода автоматически обновляться или перерисовываться в ответ на изменения в потоке данных, к которым он привязан. Поток данных – любая последовательность событий из любого источника, упорядоченная во времени. Основная идея реактивности строится на паттерне Observer. Это поведенческий паттерн проектирования, который создаёт механизм подписки, позволяющий другим следить и реагировать на события, происходящие в источнике

[Оглавление - Задачи 🔼](#menu)

<div id="a4"></div>

## 4. DRY (Don't Repeat Yourself):  Не повторяться

Принцип DRY заключается в том, чтобы избегать дублирования кода. Вместо повторения одних и тех же фрагментов кода они следует выносить в отдельные функции, классы или модули. Это улучшает читаемость кода, снижает вероятность ошибок и упрощает его поддержку и изменение.

```jsx
// Плохо
function calculateAreaOfRectangle(length, width) {
  return length * width;
}

function calculatePerimeterOfRectangle(length, width) {
  return 2 * (length + width);
}

// Лучше
function calculateRectangleProperties(length, width) {
  const area = length * width;
  const perimeter = 2 * (length + width);
  return { area, perimeter };
}
```

[Оглавление - Задачи 🔼](#menu)

<div id="a5"></div>

## 5. KISS (Keep It Simple, Stupid): Быть проще

Принцип KISS подразумевает, что код должен быть максимально простым и понятным. Сложность должна быть минимальной, а структура кода - прямолинейной и легко воспринимаемой. Это делает код более поддерживаемым и менее подверженным ошибкам.

```js
// Плохо
function complicatedAlgorithm(a, b, c, d) {
  // Сложный и труднопонимаемый алгоритм
}

// Лучше
function simpleAlgorithm(a, b) {
  // Простой и понятный алгоритм
}
```

[Оглавление - Задачи 🔼](#menu)

<div id="a6"></div>

## 6. YAGNI (You Aren’t Gonna Need It) / Вам это не понадобится

Принцип YAGNI заключается в том, чтобы не добавлять в код ненужные функции, компоненты или возможности на основе предположений о том, что они могут быть полезны в будущем. Вместо этого следует добавлять только те функции, которые реально необходимы на текущий момент. Это уменьшает избыточность кода и упрощает его понимание.

```jsx
// Плохо
function squareRoot(num) {
  // Предположение о том, что мы можем понадобиться квадратный корень
  return Math.sqrt(num);
}

// Лучше
// Добавляем функцию, только когда она действительно нужна
```

[Оглавление - Задачи 🔼](#menu)

<div id="a7"></div>

## 7. Big Design Up Front (BDUF) / Глобальное проектирование прежде всего

Прежде чем переходить к реализации, убедитесь, что все хорошо продумано.

[Оглавление - Задачи 🔼](#menu)

<div id="a8"></div>

## 8. Avoid Premature Optimization / Избегайте преждевременной оптимизации

"Преждевременная оптимизация — корень всех зол."

Avoid Premature Optimization — это принцип программирования, который гласит, что не следует пытаться оптимизировать код на ранних стадиях разработки, пока не будут выявлены реальные узкие места производительности. Идея заключается в том, что слишком ранняя оптимизация кода может привести к его усложнению, ухудшению читаемости и поддерживаемости, а также к тому, что время и усилия будут потрачены на оптимизацию тех участков программы, которые не являются критичными для производительности.

[Оглавление - Задачи 🔼](#menu)

<div id="a9"></div>

## 9. Бритва Оккама / Не создавайте ненужных сущностей без необходимости

Бритва Оккама — это философский принцип, который гласит, что из нескольких гипотез, объясняющих одно и то же явление, предпочтение следует отдавать той, которая требует наименьшего числа предположений или является самой простой.

Простота — предпочтительнее сложности

[Оглавление - Задачи 🔼](#menu)

<div id="a10"></div>

## 10. GoF (Gang of Four) (паттерны проектирования)

**GoF** (Gang of Four) — это группа авторов, которые написали одну из самых влиятельных книг по объектно-ориентированному проектированию: *"Design Patterns: Elements of Reusable Object-Oriented Software"*, опубликованную в 1994 году. Эта книга описывает 23 ключевых шаблона проектирования (design patterns), которые помогают решать распространённые проблемы в разработке ПО.

Четыре автора (или "банда четырёх") — это:
- **Эрих Гамма** (Erich Gamma),
- **Ричард Хелм** (Richard Helm),
- **Ральф Джонсон** (Ralph Johnson),
- **Джон Влиссидес** (John Vlissides).

### Шаблоны проектирования из книги GoF разделены на три категории:

#### 1. **Порождающие (Creational)**:
   - **Factory Method (Фабричный метод)**: Шаблон, который определяет интерфейс для создания объектов, позволяя подклассам решать, какой класс инстанцировать.
   - **Abstract Factory (Абстрактная фабрика)**: Предоставляет интерфейс для создания семейств взаимосвязанных объектов без указания их конкретных классов.
   - **Singleton (Одиночка)**: Гарантирует, что у класса есть только один экземпляр, и предоставляет глобальную точку доступа к нему.
   - **Builder (Строитель)**: Разделяет процесс создания сложного объекта от его представления, позволяя создавать разные представления с помощью одного и того же процесса.
   - **Prototype (Прототип)**: Позволяет создавать объекты путем клонирования существующих объектов (прототипов).

#### 2. **Структурные (Structural)**:
   - **Adapter (Адаптер)**: Преобразует интерфейс класса в другой интерфейс, который ожидает клиент. Позволяет классам с несовместимыми интерфейсами работать вместе.
   - **Bridge (Мост)**: Отделяет абстракцию от её реализации, позволяя изменять их независимо друг от друга.
   - **Composite (Компоновщик)**: Позволяет объединять объекты в древовидные структуры для представления иерархии "часть-целое". Клиенты могут работать с составными и единичными объектами одинаково.
   - **Decorator (Декоратор)**: Динамически добавляет новые функциональности объектам, не изменяя их классов.
   - **Facade (Фасад)**: Предоставляет простой интерфейс к сложной системе классов, библиотек или фреймворков.
   - **Flyweight (Приспособленец)**: Использует совместное использование объектов для эффективной работы с большим количеством мелких объектов.
   - **Proxy (Заместитель)**: Предоставляет объект-заместитель, который контролирует доступ к другому объекту, например, для оптимизации или безопасности.

#### 3. **Поведенческие (Behavioral)**:
   - **Chain of Responsibility (Цепочка обязанностей)**: Позволяет передавать запросы по цепочке обработчиков, пока один из них не обработает запрос.
   - **Command (Команда)**: Инкапсулирует запрос как объект, позволяя задавать параметры клиентов для выполнения операций, откладывать или ставить их в очередь.
   - **Interpreter (Интерпретатор)**: Определяет представление грамматики и интерпретатор для предложений в этом языке.
   - **Iterator (Итератор)**: Предоставляет способ поочередного доступа к элементам коллекции, не раскрывая её внутреннюю структуру.
   - **Mediator (Посредник)**: Определяет объект, который управляет взаимодействием между другими объектами, уменьшая связанность между ними.
   - **Memento (Хранитель)**: Позволяет сохранять и восстанавливать состояние объекта, не нарушая инкапсуляцию.
   - **Observer (Наблюдатель)**: Определяет зависимость "один ко многим" между объектами так, что при изменении состояния одного объекта все зависимые от него оповещаются.
   - **State (Состояние)**: Позволяет объекту изменять свое поведение при изменении его внутреннего состояния.
   - **Strategy (Стратегия)**: Определяет семейство алгоритмов и делает их взаимозаменяемыми, позволяя изменять алгоритм независимо от клиента, который его использует.
   - **Template Method (Шаблонный метод)**: Определяет основу алгоритма, позволяя подклассам переопределять отдельные шаги алгоритма, не изменяя его структуру.
   - **Visitor (Посетитель)**: Позволяет добавлять новые операции к объектам без изменения их классов, определяя операцию как отдельный класс.

[Оглавление - Задачи 🔼](#menu)

<div id="a11"></div>

## 11. GRASP (General Responsibility Assignment Software Patterns)

**GRASP (General Responsibility Assignment Software Patterns)** — это набор принципов и шаблонов проектирования, разработанный для того, чтобы помочь разработчикам в правильном распределении обязанностей между объектами в объектно-ориентированной системе. В отличие от шаблонов GoF, которые в основном ориентированы на решение конкретных архитектурных проблем, GRASP концентрируется на принципах назначения обязанностей (responsibilities) объектам, чтобы сделать систему более гибкой и поддерживаемой.

### Основные принципы GRASP:

1. **Information Expert (Информационный эксперт)**
   Этот принцип гласит, что обязанность должна быть назначена тому объекту, который обладает всей необходимой информацией для её выполнения. Это позволяет свести к минимуму зависимости между объектами и сделать систему более логичной.

   **Пример**: Если у объекта "Заказ" есть информация о своих товарах, логично назначить ему ответственность за расчёт общей стоимости заказа, а не выносить эту логику в другой объект.

2. **Creator (Создатель)**
   Объект должен создавать другие объекты, если он:
   - Является агрегатором этих объектов,
   - Использует их,
   - Имеет необходимую информацию для их создания.

   **Пример**: Класс "Фабрика" может быть ответственным за создание объектов определённого типа, потому что он имеет доступ к информации о том, как эти объекты создаются.

3. **Controller (Контроллер)**
   Контроллер — это объект, ответственный за обработку входящих запросов и передачу их к соответствующим бизнес-объектам. Контроллеры чаще всего применяются на уровне приложения или взаимодействия с интерфейсами пользователя, чтобы отделить логику взаимодействия от бизнес-логики.

   **Пример**: В веб-приложении контроллер принимает HTTP-запросы от пользователя, обрабатывает их и вызывает соответствующие методы на бизнес-объектах.

4. **Low Coupling (Низкая связанность)**
   Принцип низкой связанности заключается в том, чтобы минимизировать зависимости между объектами. Это делает систему более гибкой и устойчивой к изменениям, так как изменения в одном объекте не будут сильно влиять на другие.

   **Пример**: Взаимодействие между объектами может происходить через интерфейсы или абстракции, что снижает прямую зависимость одного объекта от деталей реализации другого.

5. **High Cohesion (Высокая связанность)**
   Объект должен иметь чётко определённые обязанности, которые логически связаны друг с другом. Высокая связность означает, что объект занимается конкретной задачей или областью ответственности. Это помогает поддерживать код более чистым и легким для понимания.

   **Пример**: Класс, ответственный за обработку заказов, не должен также управлять пользователями или логикой аутентификации — каждая обязанность должна быть строго определена.

6. **Polymorphism (Полиморфизм)**
   Принцип полиморфизма предполагает, что для выполнения одной и той же обязанности можно использовать разные объекты, если они имеют общее поведение через интерфейс или абстрактный класс.

   **Пример**: Если разные виды платежей (кредитная карта, PayPal, банковский перевод) реализуют один интерфейс `Payment`, то объект может работать с ними полиморфно, вызывая одинаковый метод `processPayment()`, не зная точную реализацию.

7. **Pure Fabrication (Чистая фабрикация)**
   Иногда для поддержания низкой связанности или высокой связности создаются объекты, которые не представляют собой реальных сущностей доменной модели, но выполняют важные вспомогательные задачи. Эти объекты называются "чистой фабрикацией".

   **Пример**: Логирование или отправка сообщений могут быть вынесены в отдельный объект или класс, не являющийся частью бизнес-логики, чтобы снизить связанность основных классов.

8. **Indirection (Косвенность)**
   Принцип косвенности говорит о том, что для уменьшения зависимости между объектами можно использовать посредников. Это позволяет снизить связанность и повысить гибкость системы.

   **Пример**: Внедрение уровня сервисов между контроллерами и репозиториями данных. Это позволяет менять реализацию доступа к данным без необходимости менять бизнес-логику.

9. **Protected Variations (Защищённые вариации)**
   Этот принцип заключается в том, чтобы защищать участки кода от изменения и нестабильности. Это достигается путём создания устойчивых интерфейсов и абстракций, которые позволяют изменять внутреннюю реализацию без изменения внешних зависимостей.

   **Пример**: Использование интерфейсов для работы с внешними библиотеками или сервисами. Это позволяет в будущем заменить реализацию, не нарушая основной код.

[Оглавление - Задачи 🔼](#menu)
